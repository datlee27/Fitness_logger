<UserControl x:Class="FitnessTracker.WPF.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="40">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,40,0,40">
                <Ellipse Width="80" Height="80" Margin="0,0,0,16">
                    <Ellipse.Fill>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#F97316" Offset="0"/>
                            <GradientStop Color="#22C55E" Offset="1"/>
                        </LinearGradientBrush>
                    </Ellipse.Fill>
                </Ellipse>
                <TextBlock Text="Fitness Tracker" 
                           FontSize="32" 
                           FontWeight="Bold" 
                           Foreground="{StaticResource TextPrimaryBrush}"
                           HorizontalAlignment="Center"/>
                <TextBlock Text="Theo dõi và cải thiện sức khỏe của bạn" 
                           FontSize="16" 
                           Foreground="{StaticResource TextSecondaryBrush}"
                           HorizontalAlignment="Center"
                           Margin="0,8,0,0"/>
                           
                <!-- AI Status Indicator -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,16,0,0">
                    <Ellipse Width="8" Height="8" Margin="0,0,8,0">
                        <Ellipse.Fill>
                            <SolidColorBrush Color="{Binding IsAIEnabled, Converter={StaticResource BoolToColorConverter}, ConverterParameter=#22C55E|#64748B}"/>
                        </Ellipse.Fill>
                    </Ellipse>
                    <TextBlock FontSize="12" Foreground="{StaticResource TextSecondaryBrush}">
                        <Run Text="AI "/>
                        <Run Text="{Binding IsAIEnabled, Converter={StaticResource BoolToStatusConverter}}"/>
                    </TextBlock>
                </StackPanel>
            </StackPanel>

            <!-- Stats Cards -->
            <Grid Grid.Row="1" Margin="0,0,0,40">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{StaticResource CardStyle}" Margin="0,0,10,0">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#FED7AA" Offset="0"/>
                            <GradientStop Color="#FDBA74" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <StackPanel>
                        <TextBlock Text="Tổng buổi tập" FontSize="14" Foreground="#9A3412" Margin="0,0,0,8"/>
                        <TextBlock Text="{Binding TotalSessions}" FontSize="32" FontWeight="Bold" Foreground="#9A3412"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Style="{StaticResource CardStyle}" Margin="5,0">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#BFDBFE" Offset="0"/>
                            <GradientStop Color="#93C5FD" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <StackPanel>
                        <TextBlock Text="Tổng calories" FontSize="14" Foreground="#1E3A8A" Margin="0,0,0,8"/>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding TotalCalories}" FontSize="32" FontWeight="Bold" Foreground="#1E3A8A"/>
                            <TextBlock Text=" cal" FontSize="16" Foreground="#1E3A8A" VerticalAlignment="Bottom" Margin="4,0,0,4"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <Border Grid.Column="2" Style="{StaticResource CardStyle}" Margin="10,0,0,0">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#BBF7D0" Offset="0"/>
                            <GradientStop Color="#86EFAC" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <StackPanel>
                        <TextBlock Text="Tổng thời gian" FontSize="14" Foreground="#14532D" Margin="0,0,0,8"/>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding TotalDuration}" FontSize="32" FontWeight="Bold" Foreground="#14532D"/>
                            <TextBlock Text=" phút" FontSize="16" Foreground="#14532D" VerticalAlignment="Bottom" Margin="4,0,0,4"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Menu Cards -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Start Workout -->
                <Border Grid.Column="0" Grid.Row="0" Style="{StaticResource CardStyle}" Margin="0,0,10,10" Cursor="Hand">
                    <Border.InputBindings>
                        <MouseBinding MouseAction="LeftClick" Command="{Binding StartWorkoutCommand}"/>
                    </Border.InputBindings>
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#F97316" Offset="0"/>
                            <GradientStop Color="#EA580C" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <StackPanel Padding="20">
                        <TextBlock Text="💪" FontSize="48" Margin="0,0,0,16"/>
                        <TextBlock Text="Bắt đầu tập luyện" FontSize="24" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,8"/>
                        <TextBlock Text="Bắt đầu buổi tập mới" FontSize="14" Foreground="#FED7AA" TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>

                <!-- Add Exercise -->
                <Border Grid.Column="1" Grid.Row="0" Style="{StaticResource CardStyle}" Margin="10,0,0,10" Cursor="Hand">
                    <Border.InputBindings>
                        <MouseBinding MouseAction="LeftClick" Command="{Binding AddExerciseCommand}"/>
                    </Border.InputBindings>
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#22C55E" Offset="0"/>
                            <GradientStop Color="#16A34A" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <StackPanel Padding="20">
                        <TextBlock Text="➕" FontSize="48" Margin="0,0,0,16"/>
                        <TextBlock Text="Thêm bài tập" FontSize="24" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,8"/>
                        <TextBlock Text="Tạo bài tập tùy chỉnh" FontSize="14" Foreground="#BBF7D0" TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>

                <!-- Reports -->
                <Border Grid.Column="0" Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,10,10,0" Cursor="Hand">
                    <Border.InputBindings>
                        <MouseBinding MouseAction="LeftClick" Command="{Binding ViewReportsCommand}"/>
                    </Border.InputBindings>
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#3B82F6" Offset="0"/>
                            <GradientStop Color="#2563EB" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <StackPanel Padding="20">
                        <TextBlock Text="📊" FontSize="48" Margin="0,0,0,16"/>
                        <TextBlock Text="Báo cáo" FontSize="24" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,8"/>
                        <TextBlock Text="Xem thống kê của bạn" FontSize="14" Foreground="#BFDBFE" TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>

                <!-- History -->
                <Border Grid.Column="1" Grid.Row="1" Style="{StaticResource CardStyle}" Margin="10,10,0,0" Cursor="Hand">
                    <Border.InputBindings>
                        <MouseBinding MouseAction="LeftClick" Command="{Binding ViewHistoryCommand}"/>
                    </Border.InputBindings>
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#A855F7" Offset="0"/>
                            <GradientStop Color="#9333EA" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <StackPanel Padding="20">
                        <TextBlock Text="📖" FontSize="48" Margin="0,0,0,16"/>
                        <TextBlock Text="Nhật ký tập luyện" FontSize="24" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,8"/>
                        <TextBlock Text="Các bài tập đã lưu" FontSize="14" Foreground="#E9D5FF" TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </ScrollViewer>
</UserControl>
