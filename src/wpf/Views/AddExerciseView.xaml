<UserControl x:Class="FitnessTracker.WPF.Views.AddExerciseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="40">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
                <Button Content="←" Command="{Binding GoBackCommand}" Style="{StaticResource OutlineButtonStyle}" Padding="12" Margin="0,0,12,0"/>
                <TextBlock Text="Thêm bài tập mới" FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"/>
            </StackPanel>

            <Border Grid.Row="1" Style="{StaticResource CardStyle}" MaxWidth="800" HorizontalAlignment="Left">
                <StackPanel>
                    <TextBlock Text="Tên bài tập *" Margin="0,0,0,8"/>
                    <TextBox Text="{Binding Name}" Padding="12" Margin="0,0,0,20"/>

                    <TextBlock Text="Vùng cơ *" Margin="0,0,0,8"/>
                    <ComboBox ItemsSource="{Binding MuscleGroups}" SelectedItem="{Binding SelectedMuscleGroup}" Padding="12" Margin="0,0,0,20"/>

                    <TextBlock Text="Môi trường *" Margin="0,0,0,8"/>
                    <ComboBox ItemsSource="{Binding Environments}" SelectedItem="{Binding SelectedEnvironment}" Padding="12" Margin="0,0,0,20"/>

                    <TextBlock Text="Độ khó" Margin="0,0,0,8"/>
                    <ComboBox ItemsSource="{Binding Difficulties}" SelectedItem="{Binding SelectedDifficulty}" Padding="12" Margin="0,0,0,20"/>

                    <TextBlock Text="Hướng dẫn chi tiết *" Margin="0,0,0,8"/>
                    <TextBox Text="{Binding Instructions}" Padding="12" MinHeight="100" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,0,0,20"/>

                    <UniformGrid Columns="3" Rows="1">
                        <StackPanel Margin="0,0,8,0">
                            <TextBlock Text="Số reps" Margin="0,0,0,8"/>
                            <TextBox Text="{Binding Reps}" Padding="12"/>
                        </StackPanel>
                        <StackPanel Margin="4,0">
                            <TextBlock Text="Calories" Margin="0,0,0,8"/>
                            <TextBox Text="{Binding Calories}" Padding="12"/>
                        </StackPanel>
                        <StackPanel Margin="8,0,0,0">
                            <TextBlock Text="Thời gian (phút)" Margin="0,0,0,8"/>
                            <TextBox Text="{Binding Duration}" Padding="12"/>
                        </StackPanel>
                    </UniformGrid>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,40,0,0">
                        <Button Content="Hủy" Command="{Binding GoBackCommand}" Style="{StaticResource OutlineButtonStyle}" Margin="0,0,12,0" MinWidth="120"/>
                        <Button Content="Lưu bài tập" Command="{Binding SaveCommand}" Style="{StaticResource PrimaryButtonStyle}" MinWidth="120"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>
