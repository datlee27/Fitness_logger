<UserControl x:Class="FitnessTracker.WPF.Views.WorkoutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="40">
            <!-- Environment Selection Step -->
            <StackPanel Visibility="{Binding CurrentStep, Converter={StaticResource StepVisibilityConverter}, ConverterParameter=Environment}">
                <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                    <Button Content="←" Command="{Binding GoBackCommand}" Style="{StaticResource OutlineButtonStyle}" Padding="12" Margin="0,0,12,0"/>
                    <TextBlock Text="Chọn môi trường tập luyện" FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                </StackPanel>

                <Grid Margin="0,40,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Style="{StaticResource CardStyle}" Margin="0,0,10,0" Cursor="Hand">
                        <Border.InputBindings>
                            <MouseBinding MouseAction="LeftClick" Command="{Binding SelectEnvironmentCommand}" CommandParameter="Ở nhà"/>
                        </Border.InputBindings>
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#F97316" Offset="0"/>
                                <GradientStop Color="#EA580C" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <StackPanel Padding="40">
                            <TextBlock Text="🏠" FontSize="64" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                            <TextBlock Text="Ở nhà" FontSize="28" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,12"/>
                            <TextBlock Text="Bài tập không cần dụng cụ hoặc dụng cụ đơn giản" FontSize="14" Foreground="#FED7AA" TextWrapping="Wrap" TextAlignment="Center"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="1" Style="{StaticResource CardStyle}" Margin="10,0,0,0" Cursor="Hand">
                        <Border.InputBindings>
                            <MouseBinding MouseAction="LeftClick" Command="{Binding SelectEnvironmentCommand}" CommandParameter="Phòng gym"/>
                        </Border.InputBindings>
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#3B82F6" Offset="0"/>
                                <GradientStop Color="#2563EB" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <StackPanel Padding="40">
                            <TextBlock Text="🏋️" FontSize="64" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                            <TextBlock Text="Phòng gym" FontSize="28" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,12"/>
                            <TextBlock Text="Bài tập sử dụng máy móc và thiết bị chuyên nghiệp" FontSize="14" Foreground="#BFDBFE" TextWrapping="Wrap" TextAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <Border Style="{StaticResource CardStyle}" Margin="0,20,0,0">
                    <StackPanel>
                        <TextBlock Text="Mục tiêu của bạn" FontSize="16" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,12"/>
                        <ListBox ItemsSource="{Binding Goals}" SelectedItem="{Binding SelectedGoal}" Background="Transparent" BorderThickness="0">
                            <ListBox.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Rows="1"/>
                                </ItemsPanelTemplate>
                            </ListBox.ItemsPanel>
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border Padding="20,12" Background="{StaticResource PrimaryBrush}" CornerRadius="8" Margin="0,0,8,0" Cursor="Hand">
                                        <TextBlock Text="{Binding}" Foreground="White" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </StackPanel>
                </Border>

                <Button Content="Tiếp tục" Command="{Binding ContinueToMuscleSelectionCommand}" Style="{StaticResource PrimaryButtonStyle}" Margin="0,20,0,0" HorizontalAlignment="Right" MinWidth="200"/>
            </StackPanel>

            <!-- Rest of the steps will be similar... For brevity, I'll create a simplified version -->
            <StackPanel Visibility="{Binding CurrentStep, Converter={StaticResource StepVisibilityConverter}, ConverterParameter=Select}">
                <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                    <Button Content="←" Command="{Binding GoBackCommand}" Style="{StaticResource OutlineButtonStyle}" Padding="12" Margin="0,0,12,0"/>
                    <TextBlock Text="Chọn nhóm cơ và bài tập" FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                </StackPanel>

                <Border Style="{StaticResource CardStyle}" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="Chọn nhóm cơ" FontSize="16" Margin="0,0,0,8"/>
                        <ComboBox ItemsSource="{Binding MuscleGroups}" SelectedItem="{Binding SelectedMuscleGroup}"/>
                        <Button Content="AI Đề xuất tối ưu ✨" Command="{Binding AISuggestWorkoutCommand}" Style="{StaticResource PrimaryButtonStyle}" Margin="0,12,0,0"/>
                    </StackPanel>
                </Border>

                <TextBlock Text="Bài tập đã chọn:" FontSize="18" Margin="0,0,0,12"/>
                <ListBox ItemsSource="{Binding SelectedExercises}" MinHeight="200">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource CardStyle}" Margin="0,4">
                                <TextBlock Text="{Binding Exercise.Name}"/>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <Border Style="{StaticResource CardStyle}" Margin="0,20,0,0">
                    <StackPanel>
                        <TextBlock Text="Tổng quan" FontSize="18" Margin="0,0,0,12"/>
                        <UniformGrid Columns="3" Rows="1">
                            <StackPanel Margin="0,0,8,0">
                                <TextBlock Text="Calories" Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBlock Text="{Binding TotalCalories}" FontSize="24" FontWeight="Bold"/>
                            </StackPanel>
                            <StackPanel Margin="4,0">
                                <TextBlock Text="Thời gian" Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBlock Text="{Binding EstimatedTime}" FontSize="24" FontWeight="Bold"/>
                            </StackPanel>
                            <StackPanel Margin="8,0,0,0">
                                <TextBlock Text="Sets" Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBlock Text="{Binding TotalSets}" FontSize="24" FontWeight="Bold"/>
                            </StackPanel>
                        </UniformGrid>
                    </StackPanel>
                </Border>

                <Button Content="Bắt đầu tập luyện" Command="{Binding StartWorkoutCommand}" Style="{StaticResource PrimaryButtonStyle}" Margin="0,20,0,0" HorizontalAlignment="Right" MinWidth="200"/>
            </StackPanel>

            <!-- Active Workout Step -->
            <Grid Visibility="{Binding CurrentStep, Converter={StaticResource StepVisibilityConverter}, ConverterParameter=Active}">
                <Grid.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#F97316" Offset="0"/>
                        <GradientStop Color="#EA580C" Offset="1"/>
                    </LinearGradientBrush>
                </Grid.Background>
                <StackPanel Margin="40" VerticalAlignment="Center">
                    <TextBlock Text="{Binding CurrentWorkoutExercise.Exercise.Name}" FontSize="32" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                    <Border Background="White" CornerRadius="12" Padding="40">
                        <StackPanel>
                            <TextBlock Text="Reps mục tiêu" FontSize="14" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center"/>
                            <TextBlock Text="{Binding CurrentWorkoutExercise.Reps}" FontSize="48" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}" HorizontalAlignment="Center" Margin="0,8,0,20"/>
                            <TextBlock FontSize="18" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center">
                                <Run Text="Set "/>
                                <Run Text="{Binding CurrentSet}"/>
                                <Run Text=" / "/>
                                <Run Text="{Binding CurrentWorkoutExercise.Sets}"/>
                            </TextBlock>
                        </StackPanel>
                    </Border>
                    <Button Content="Hoàn thành Set" Command="{Binding CompleteSetCommand}" Style="{StaticResource SecondaryButtonStyle}" Margin="0,20,0,0" MinWidth="300" HorizontalAlignment="Center"/>
                </StackPanel>
            </Grid>

            <!-- Rest Step -->
            <Grid Visibility="{Binding CurrentStep, Converter={StaticResource StepVisibilityConverter}, ConverterParameter=Rest}">
                <Grid.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#3B82F6" Offset="0"/>
                        <GradientStop Color="#2563EB" Offset="1"/>
                    </LinearGradientBrush>
                </Grid.Background>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="⏱️" FontSize="64" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                    <TextBlock Text="Thời gian nghỉ" FontSize="28" Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                    <TextBlock Text="{Binding RestTimeDisplay}" FontSize="72" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                    <TextBlock Text="Uống nước và thư giãn cơ bắp" FontSize="16" Foreground="#BFDBFE" HorizontalAlignment="Center" Margin="0,0,0,40"/>
                    <Button Content="Bỏ qua nghỉ ngơi" Command="{Binding SkipRestCommand}" Background="White" Foreground="{StaticResource AccentBrush}" Padding="20,12" MinWidth="200" HorizontalAlignment="Center"/>
                </StackPanel>
            </Grid>

            <!-- Complete Step -->
            <StackPanel Visibility="{Binding CurrentStep, Converter={StaticResource StepVisibilityConverter}, ConverterParameter=Complete}">
                <TextBlock Text="🎉 Xuất sắc!" FontSize="32" FontWeight="Bold" Foreground="{StaticResource SecondaryBrush}" HorizontalAlignment="Center" Margin="0,40,0,40"/>
                
                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="Kết quả buổi tập" FontSize="20" Margin="0,0,0,20"/>
                        <UniformGrid Columns="2" Rows="2">
                            <StackPanel Margin="0,0,8,8">
                                <TextBlock Text="🔥 Calories" Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBlock Text="{Binding TotalCalories}" FontSize="28" FontWeight="Bold"/>
                            </StackPanel>
                            <StackPanel Margin="8,0,0,8">
                                <TextBlock Text="⏰ Thời gian" Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBlock Text="{Binding EstimatedTime}" FontSize="28" FontWeight="Bold"/>
                            </StackPanel>
                            <StackPanel Margin="0,8,8,0">
                                <TextBlock Text="🔄 Sets" Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBlock Text="{Binding TotalSets}" FontSize="28" FontWeight="Bold"/>
                            </StackPanel>
                        </UniformGrid>

                        <TextBlock Text="Đặt tên buổi tập (tùy chọn)" Margin="0,20,0,8"/>
                        <TextBox Text="{Binding WorkoutName}" Padding="12"/>
                    </StackPanel>
                </Border>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                    <Button Content="Không lưu" Command="{Binding SaveWorkoutCommand}" CommandParameter="False" Style="{StaticResource OutlineButtonStyle}" Margin="0,0,12,0"/>
                    <Button Content="Lưu lại" Command="{Binding SaveWorkoutCommand}" CommandParameter="True" Style="{StaticResource PrimaryButtonStyle}"/>
                </StackPanel>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</UserControl>
