<Window x:Class="FitnessTracker.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodels="clr-namespace:FitnessTracker.WPF.ViewModels"
        xmlns:views="clr-namespace:FitnessTracker.WPF.Views"
        mc:Ignorable="d"
        Title="{Binding CurrentPageTitle}" 
        Height="800" Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}">
    
    <Window.Resources>
        <!-- DataTemplates for ViewModels -->
        <DataTemplate DataType="{x:Type viewmodels:DashboardViewModel}">
            <views:DashboardView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:WorkoutViewModel}">
            <views:WorkoutView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:AddExerciseViewModel}">
            <views:AddExerciseView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:ReportsViewModel}">
            <views:ReportsView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:HistoryViewModel}">
            <views:HistoryView/>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <!-- Main Content -->
        <ContentControl Content="{Binding CurrentView}"/>
    </Grid>
</Window>
